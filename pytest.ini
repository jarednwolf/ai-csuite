[pytest]
markers =
    e2e: end-to-end tests that hit a running orchestrator at ORCH_BASE (default http://localhost:8000)
    requires_github: tests that exercise live GitHub PR flow (need GITHUB_TOKEN and E2E_REPO_* env vars)


